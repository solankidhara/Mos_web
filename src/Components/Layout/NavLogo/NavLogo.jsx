import { Navbar } from 'react-bootstrap';

const NavLogo = () => {
      return (
            <Navbar.Brand href="/">
                  <img alt="logo" src="./images/Logo.png" className="inline-block align-top" />
            </Navbar.Brand>
      );
};

export default NavLogo;
